apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.appName }}-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: {{ .Values.appName }}-tls-secret
  issuerRef:
    name: {{ .Values.CAIssuer }}
    kind: ClusterIssuer
  dnsNames:
  - {{ .Values.appName }}.local
